@model ECommerce.Entities.Category

@{
    ViewData["Title"] = "DeleteCategory";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div>
    <h3>Are you sure you want to delete this category?</h3>
    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                Name
            </dt>
            <dd class="col-sm-10">
                @Model.Name
            </dd>
        </dl>
    </div>

    <form asp-action="DeleteCategoryConfirmed" method="post">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <input type="submit" value="Delete" asp-controller="Admin" asp-action="DeleteCategory" class="btn btn-danger" />
            <a href="@Url.Action("Categories")" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>
